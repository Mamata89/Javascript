//DATA TYPES        
    // 1. Srings   =  Single Quote, double  quote, backtick

            const firstName = "mamata"
            const lastName="Moger"
            console.log(`firstname ${firstName} ad lastName ${lastName}`)   // firstname mamata ad lastName Moger

    // 2. Numbers

    //3. Boolean
            const dataChecked = true;
            const valueCounted = false;

    //4. undefined
            let name
            console.log(name)  // undefined

            let str=undefined
            console.log(str)   // undefined

    //5. null
            const number = null
        
    //6. symbol
            const value1 = Symbol("Mamata")
            const value2 = Symbol("Mamata")
            let result = (value1 === value2) ? true : false;  
            console.log(result)   //false

    //7. Objects